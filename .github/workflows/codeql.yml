name: CodeQL

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "23 3 * * 1"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref || github.run_id }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

permissions: {}

env:
  DEBIAN_BASE_IMAGE: debian:trixie@sha256:fac46bff2e02f51425b6e33b0e1169f55dfb053d83511ca28aa50c09fd5ed7a4
  DEBIAN_SNAPSHOT_URL: http://snapshot.debian.org/archive/debian
  DEBIAN_SNAPSHOT_TIMESTAMP: 20260721T000000Z
  DEBIAN_SNAPSHOT_INRELEASE_SHA256: 98b25b5cd185c59d34aa6e4c3e9b5b8f01bbe9d104fe2dcfbcd30dc0a14a59ed
  DEBIAN_SUITE: trixie

jobs:
  analyze:
    name: analyze (${{ matrix.language }})
    runs-on: ubuntu-24.04
    container:
      image: debian:trixie@sha256:fac46bff2e02f51425b6e33b0e1169f55dfb053d83511ca28aa50c09fd5ed7a4
    permissions:
      actions: read # Resolve workflow metadata when analyzing GitHub Actions definitions.
      contents: read # Check out repository contents for CodeQL analysis.
      security-events: write # Upload CodeQL SARIF results to code scanning.
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: c-cpp
            build-mode: manual

    steps:
      - name: Bootstrap pinned CI tooling
        run: |
          set -euo pipefail
          echo 'Acquire::Retries "6";' > /etc/apt/apt.conf.d/80-retries
          echo 'Acquire::Check-Valid-Until "false";' > /etc/apt/apt.conf.d/90snapshot
          rm -f /etc/apt/sources.list /etc/apt/sources.list.d/*.list /etc/apt/sources.list.d/*.sources
          printf 'deb [check-valid-until=no] %s/%s/ %s main\n' \
            "${DEBIAN_SNAPSHOT_URL}" "${DEBIAN_SNAPSHOT_TIMESTAMP}" "${DEBIAN_SUITE}" \
            > /etc/apt/sources.list.d/snapshot.list
          apt-get update
          snapshot_inrelease="/var/lib/apt/lists/snapshot.debian.org_archive_debian_${DEBIAN_SNAPSHOT_TIMESTAMP}_dists_${DEBIAN_SUITE}_InRelease"
          test -f "${snapshot_inrelease}"
          echo "${DEBIAN_SNAPSHOT_INRELEASE_SHA256}  ${snapshot_inrelease}" | sha256sum --check --strict
          apt-get install -y --no-install-recommends ca-certificates git
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
          show-progress: false

      - name: Verify CI snapshot lock matches the release builder lock
        run: |
          set -euo pipefail
          expected_base_image="${DEBIAN_BASE_IMAGE}"
          expected_snapshot_url="${DEBIAN_SNAPSHOT_URL}"
          expected_snapshot_timestamp="${DEBIAN_SNAPSHOT_TIMESTAMP}"
          expected_snapshot_inrelease_sha256="${DEBIAN_SNAPSHOT_INRELEASE_SHA256}"
          expected_suite="${DEBIAN_SUITE}"
          # shellcheck disable=SC1091
          . ./docker/release-builder.lock

          [[ "${DEBIAN_BASE_IMAGE}" == "${expected_base_image}" ]]
          [[ "${DEBIAN_SNAPSHOT_URL}" == "${expected_snapshot_url}" ]]
          [[ "${DEBIAN_SNAPSHOT_TIMESTAMP}" == "${expected_snapshot_timestamp}" ]]
          [[ "${DEBIAN_SNAPSHOT_INRELEASE_SHA256}" == "${expected_snapshot_inrelease_sha256}" ]]
          [[ "${DEBIAN_SUITE}" == "${expected_suite}" ]]

      - name: Install C/C++ dependencies
        if: matrix.language == 'c-cpp'
        run: ./scripts/install-ci-deps.sh codeql-c-cpp

      - name: Initialize CodeQL
        uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          config: |
            threat-models: local

      - name: Build for CodeQL
        if: matrix.language == 'c-cpp'
        run: |
          make clean
          make CC=gcc
          make CC=gcc test

      - name: Analyze
        uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          category: "/language:${{ matrix.language }}"
